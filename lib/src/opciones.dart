import 'package:flutter/material.dart';

class optionSccreen extends StatelessWidget {
  const optionSccreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold
    (
      appBar: AppBar(title:Text('Opciones')),

      body: Column(
        children: <Widget>[

          Container(
            
          )

        ],
      ),

      
    );
  }
}